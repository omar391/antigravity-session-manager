- @workflow.json#L45-54 Lets do something about our "dynamic" blocks. Here the prposal, on detection of a new correct dynamic, we create a variant by that stactic name and its correct cached x,y so on the next run we can check for cache x,y for that dynamic val and varify as usual like we do for cached axix values. This should save us some time for dyn slots

- nested text and image strcuture in worflow json except cached x|y pos.
